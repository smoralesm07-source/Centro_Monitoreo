(function(){
'use strict';
const BUILD='20260915g-analyst';
const ANALYSIS='https://ldmtlwzqaqmegedktlxr.supabase.co/functions/v1/mercado-publico-analysis-pilot';
const css=`
.mp-analyst-search{margin-bottom:14px;padding:20px;background:linear-gradient(180deg,rgba(45,212,167,.045),transparent),var(--panel);border:1px solid var(--line);border-radius:16px}
.mp-analyst-search h2{margin:0 0 5px;font-size:22px}.mp-analyst-search p{margin:0;color:var(--ink2);max-width:960px;line-height:1.45}.mp-searchline{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;margin-top:16px}.mp-searchline input{padding:13px 14px;font-size:15px}
.mp-modes{display:flex;gap:6px;flex-wrap:wrap;margin-top:9px}.mp-mode{background:transparent;color:var(--ink2);padding:7px 10px}.mp-mode.active{background:var(--raised);color:var(--ink);border-color:#3b4d61}.mp-panorama{margin-bottom:14px}.mp-pan-head{display:flex;align-items:flex-end;justify-content:space-between;gap:12px;margin-bottom:10px}.mp-pan-head h3{margin:0;font-size:14px}.mp-pan-head p{margin:3px 0 0;color:var(--ink3);font-size:11px}
.mp-kpis{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px}.mp-kpi{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:11px}.mp-kpi .n{font-size:20px;font-weight:750}.mp-kpi .l{font-size:9px;text-transform:uppercase;letter-spacing:.07em;color:var(--ink3);margin-top:3px}.mp-pan-grid{display:grid;grid-template-columns:1.25fr 1fr 1fr;gap:10px;margin-top:10px}.mp-box{background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:12px;min-width:0}.mp-box h4{font-size:10px;text-transform:uppercase;letter-spacing:.07em;color:var(--ink3);margin:0 0 9px}
.mp-bars{height:128px;display:flex;align-items:flex-end;gap:6px;padding-top:8px}.mp-barcol{flex:1;min-width:0;text-align:center}.mp-bar{background:var(--accent2);border-radius:4px 4px 1px 1px;min-height:3px}.mp-barlab{font-size:8px;color:var(--ink3);margin-top:5px;white-space:nowrap;overflow:hidden}.mp-rank{display:grid;gap:5px}.mp-rank button{width:100%;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;text-align:left;background:transparent;border-color:transparent;padding:6px 5px}.mp-rank button:hover{background:var(--panel2);border-color:var(--line)}.mp-rank .name{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.mp-rank .count{color:var(--accent);font-family:var(--mono)}
.mp-coverage-note{margin-top:10px;padding:8px 10px;border:1px solid #594a20;background:#211d10;border-radius:8px;color:#f1d783;font-size:10px}.mp-groups{padding:14px;display:grid;gap:10px}.mp-group-title{display:flex;justify-content:space-between;align-items:center;color:var(--ink2);font-size:11px;text-transform:uppercase;letter-spacing:.06em}.mp-result-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(270px,1fr));gap:8px}.mp-result{background:var(--panel2);border:1px solid var(--line);border-radius:10px;padding:12px;cursor:pointer}.mp-result:hover{border-color:#3b4d61;background:var(--raised)}.mp-result .kind{font-size:9px;text-transform:uppercase;letter-spacing:.06em;color:var(--blue)}.mp-result h4{font-size:12px;margin:6px 0 4px;line-height:1.3}.mp-result .sub{font-size:10px;color:var(--ink3);line-height:1.35}.mp-result .foot{display:flex;justify-content:space-between;align-items:center;margin-top:9px}.mp-result .nproc{font-size:10px;color:var(--accent)}
.mp-ctx{position:fixed;inset:0;background:rgba(2,5,9,.78);z-index:90;display:flex;justify-content:flex-end}.mp-ctx-panel{width:min(1180px,97vw);height:100%;overflow:auto;background:var(--canvas);border-left:1px solid var(--line2)}.mp-ctx-head{position:sticky;top:0;z-index:3;background:rgba(10,15,24,.97);backdrop-filter:blur(10px);border-bottom:1px solid var(--line);padding:16px 18px}.mp-ctx-title{display:flex;justify-content:space-between;gap:15px}.mp-ctx-title h2{margin:4px 0 4px;font-size:19px}.mp-ctx-kind{font-size:9px;text-transform:uppercase;letter-spacing:.08em;color:var(--accent)}.mp-ctx-actions{display:flex;gap:6px;align-items:flex-start}.mp-ctx-body{padding:14px}.mp-ctx-kpis{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px}.mp-ctx-grid{display:grid;grid-template-columns:1.2fr 1fr 1fr;gap:9px;margin-top:9px}.mp-pivot{width:100%;text-align:left;background:transparent;border-color:transparent;padding:7px}.mp-pivot:hover{background:var(--panel2);border-color:var(--line)}.mp-pivot strong{display:block;font-size:10px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.mp-pivot span{font-size:9px;color:var(--ink3)}.mp-ctx-section{margin-top:9px;background:var(--panel);border:1px solid var(--line);border-radius:12px;padding:12px}.mp-ctx-section h3{margin:0 0 9px;font-size:11px}.mp-mini-dist{display:flex;flex-wrap:wrap;gap:6px}.mp-dist{border:1px solid var(--line2);background:var(--panel2);border-radius:999px;padding:5px 8px;font-size:9px;color:var(--ink2)}.mp-money{display:flex;gap:7px;flex-wrap:wrap}.mp-money .m{background:var(--panel2);border:1px solid var(--line);border-radius:8px;padding:8px 10px}.mp-money .m strong{display:block;font-size:12px}.mp-money .m span{font-size:9px;color:var(--ink3)}.mp-proc-table{max-height:380px;overflow:auto}.mp-proc-table table{font-size:10px}.mp-proc-table tr{cursor:pointer}.mp-proc-table tr:hover td{background:var(--panel2)}.mp-backtrail{display:flex;gap:5px;flex-wrap:wrap;margin-top:7px}.mp-backtrail button{padding:4px 7px;font-size:9px;background:transparent;color:var(--ink2)}.mp-loading{opacity:.55;pointer-events:none}
@media(max-width:1100px){.mp-kpis{grid-template-columns:repeat(3,1fr)}.mp-pan-grid,.mp-ctx-grid{grid-template-columns:1fr}.mp-ctx-kpis{grid-template-columns:repeat(3,1fr)}}@media(max-width:700px){.mp-searchline{grid-template-columns:1fr}.mp-kpis,.mp-ctx-kpis{grid-template-columns:repeat(2,1fr)}}`;
const style=document.createElement('style');style.textContent=css;document.head.appendChild(style);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmtDate=v=>{if(!v)return '—';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):new Intl.DateTimeFormat('es-CL',{dateStyle:'medium'}).format(d)};
const fmtN=v=>new Intl.NumberFormat('es-CL').format(Number(v)||0);
const fmtMoney=(v,c)=>{const n=Number(v);if(!Number.isFinite(n))return '—';try{return new Intl.NumberFormat('es-CL',{style:'currency',currency:c||'CLP',maximumFractionDigits:0}).format(n)}catch{return fmtN(n)+' '+(c||'')}};
let mode='all',ctxHistory=[];
function storedSession(){try{return JSON.parse(localStorage.getItem('mp_pilot_session')||'null')}catch{return null}}
async function analysisApi(action,payload={}){const call=async()=>{const s=storedSession();if(!s?.access_token)throw new Error('Sesión requerida');return fetch(ANALYSIS,{method:'POST',headers:{'content-type':'application/json',Authorization:'Bearer '+s.access_token},body:JSON.stringify({action,...payload})})};let r=await call();if(r.status===401&&typeof window.api==='function'){await window.api('session');r=await call()}const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'No fue posible consultar el análisis');return d}
function barChart(series){if(!series?.length)return '<div class="tiny">Sin serie temporal.</div>';const max=Math.max(...series.map(x=>x.count),1);return `<div class="mp-bars">${series.map(x=>`<div class="mp-barcol" title="${esc(x.month)} · ${esc(x.count)}"><div class="mp-bar" style="height:${Math.max(4,Math.round((x.count/max)*100))}px"></div><div class="mp-barlab">${esc(x.month.slice(5))}</div></div>`).join('')}</div>`}
function rankList(rows,type){return `<div class="mp-rank">${(rows||[]).slice(0,7).map(x=>`<button data-pivot-type="${type}" data-key="${esc(x.key)}" data-label="${esc(x.label)}"><span class="name">${esc(x.label)}</span><span class="count">${esc(x.count)}</span></button>`).join('')||'<div class="tiny">Sin datos.</div>'}</div>`}
function bindPivots(root=document){root.querySelectorAll('[data-pivot-type]').forEach(b=>b.onclick=e=>{e.stopPropagation();const type=b.dataset.pivotType,key=b.dataset.key,label=b.dataset.label;if(type==='organismo'||type==='proveedor')openContext({type,rut:key,name:label});else openContext({type:'item',product_code:key,description:label})})}
async function loadOverview(){const box=document.getElementById('mpPanorama');if(!box)return;box.classList.add('mp-loading');try{renderOverview(await analysisApi('overview'))}catch(e){box.innerHTML=`<div class="note warn">No fue posible cargar el panorama: ${esc(e.message||e)}</div>`}finally{box.classList.remove('mp-loading')}}
function renderOverview(d){const s=d.summary||{},box=document.getElementById('mpPanorama');if(!box)return;box.innerHTML=`<div class="mp-pan-head"><div><h3>Panorama del índice piloto</h3><p>Vista descriptiva para orientarse antes de abrir un proceso. Sin scoring ni inferencias.</p></div><span class="tiny">${esc(d.meta?.latency_ms||'—')} ms</span></div><div class="mp-kpis"><div class="mp-kpi"><div class="n">${fmtN(s.indexed_tenders)}</div><div class="l">Licitaciones</div></div><div class="mp-kpi"><div class="n">${fmtN(s.buyer_count)}</div><div class="l">Organismos compradores</div></div><div class="mp-kpi"><div class="n">${fmtN(s.supplier_count)}</div><div class="l">Proveedores</div></div><div class="mp-kpi"><div class="n">${fmtN(s.indexed_items)}</div><div class="l">Ítems</div></div><div class="mp-kpi"><div class="n">${fmtN(s.indexed_awards)}</div><div class="l">Adjudicaciones</div></div><div class="mp-kpi"><div class="n">${fmtDate(s.min_date).replace(/ de /g,' ')}</div><div class="l">Desde</div></div></div><div class="mp-pan-grid"><div class="mp-box"><h4>Actividad por mes</h4>${barChart(d.timeline)}</div><div class="mp-box"><h4>Organismos con más procesos en la muestra</h4>${rankList(d.top_buyers,'organismo')}</div><div class="mp-box"><h4>Proveedores con más apariciones adjudicadas</h4>${rankList(d.top_suppliers,'proveedor')}</div></div><div class="mp-pan-grid"><div class="mp-box"><h4>Productos / categorías más presentes</h4>${rankList(d.top_items,'item')}</div><div class="mp-box"><h4>Estados</h4><div class="mp-mini-dist">${(d.status_distribution||[]).slice(0,8).map(x=>`<span class="mp-dist">${esc(x.key)} · ${esc(x.count)}</span>`).join('')}</div></div><div class="mp-box"><h4>Modalidades</h4><div class="mp-mini-dist">${(d.method_distribution||[]).slice(0,8).map(x=>`<span class="mp-dist">${esc(x.key)} · ${esc(x.count)}</span>`).join('')}</div></div></div><div class="mp-coverage-note">Cobertura piloto: ${esc(fmtDate(s.min_date))} → ${esc(fmtDate(s.max_date))}. Estos conteos describen sólo el índice cargado; la ficha individual se consulta en vivo desde Mercado Público.</div>`;bindPivots(box)}
function entityKey(r){if(r.match_type==='proveedor')return 'p|'+(r.participant_rut||r.participant_name||r.match_label||'');if(r.match_type==='organismo')return 'o|'+(r.buyer_rut||r.buyer_name||r.match_label||'');if(r.match_type==='item')return 'i|'+(r.product_code||r.item_description||r.match_label||'');return 'l|'+r.tender_code}
function groupResults(rows){const m=new Map();for(const r of rows||[]){const k=entityKey(r),z=m.get(k)||{type:r.match_type,rows:[],label:'',key:'',sub:''};z.rows.push(r);if(r.match_type==='proveedor'){z.label=r.participant_name||r.match_label||'Proveedor';z.key=r.participant_rut||'';z.sub=z.key?'RUT '+z.key:''}else if(r.match_type==='organismo'){z.label=r.match_label||r.buyer_name||'Organismo';z.key=r.buyer_rut||'';z.sub=z.key?'RUT '+z.key:''}else if(r.match_type==='item'){z.label=r.item_description||r.match_label||'Producto / ítem';z.key=r.product_code||'';z.sub=z.key?'UNSPSC '+z.key:''}else{z.label=r.title||r.tender_code;z.key=r.tender_code;z.sub=r.buyer_name||''}m.set(k,z)}return [...m.values()]}
function renderSearch(rows,meta){const table=document.getElementById('table'),count=document.getElementById('count'),status=document.getElementById('status'),source=document.getElementById('sourcebar'),groups=groupResults(rows);if(count)count.textContent=String(groups.length);if(status)status.textContent='Resultados agrupados por entidad';if(source)source.innerHTML=`<span class="pill ok">índice transversal</span><span class="pill">${esc(meta?.mode||mode)}</span><span class="pill">${esc(meta?.latency_ms||'—')} ms</span><span class="pill">${esc(meta?.indexed_tenders||0)} licitaciones indexadas</span>`;if(!table)return;if(!groups.length){table.innerHTML='<div class="empty">No hay coincidencias en la cobertura indexada. Prueba otro término o usa la exploración por fecha.</div>';return}const by={proveedor:[],organismo:[],item:[],licitacion:[]};groups.forEach(g=>(by[g.type]||by.licitacion).push(g));const titles={proveedor:'Proveedores',organismo:'Servicios públicos / compradores',item:'Productos e ítems',licitacion:'Licitaciones'};table.innerHTML=`<div class="mp-groups">${Object.entries(by).filter(([,v])=>v.length).map(([type,list])=>`<section><div class="mp-group-title"><span>${titles[type]}</span><span>${list.length}</span></div><div class="mp-result-grid">${list.map(g=>`<article class="mp-result" data-rtype="${type}" data-key="${esc(g.key)}" data-label="${esc(g.label)}"><div class="kind">${titles[type]}</div><h4>${esc(g.label)}</h4><div class="sub">${esc(g.sub)}</div><div class="foot"><span class="nproc">${g.rows.length} proceso(s) coincidente(s)</span><span class="tiny">${type==='licitacion'?'Abrir ficha en vivo →':'Explorar contexto →'}</span></div></article>`).join('')}</div></section>`).join('')}</div>`;table.querySelectorAll('.mp-result').forEach(el=>el.onclick=()=>{const t=el.dataset.rtype,k=el.dataset.key,l=el.dataset.label;if(t==='licitacion')window.openCode(k);else if(t==='proveedor'||t==='organismo')openContext({type:t,rut:k,name:l});else openContext({type:'item',product_code:k,description:l})})}
async function doSearch(){const q=String(document.getElementById('mpAnalystQ')?.value||'').trim();if(q.length<2)return;if(/^\d{2,8}-\d{1,5}-[A-Z0-9]{2,6}$/i.test(q)){window.openCode(q);return}const card=document.getElementById('mpAnalystSearch'),btn=document.getElementById('mpAnalystBtn');card?.classList.add('mp-loading');if(btn)btn.textContent='Buscando…';try{const d=await window.api('search',{query:q,mode,limit:180});renderSearch(d.rows||[],d.meta||{})}catch(e){const t=document.getElementById('table');if(t)t.innerHTML=`<div class="empty">${esc(e.message||e)}</div>`}finally{card?.classList.remove('mp-loading');if(btn)btn.textContent='Buscar'}}
function moneyBlocks(rows){if(!rows?.length)return '';return `<div class="mp-money">${rows.slice(0,6).map(x=>`<div class="m"><strong>${esc(fmtMoney(x.amount,x.currency==='SIN_MONEDA'?'CLP':x.currency))}</strong><span>${esc(x.currency)} · ${esc(x.count)} adjudicación(es)</span></div>`).join('')}</div>`}
function ctxRank(rows,type,title){return `<div class="mp-box"><h4>${esc(title)}</h4>${rankList(rows,type)}</div>`}
function contextTrail(){return `<div class="mp-backtrail">${ctxHistory.slice(-5).map((x,i)=>`<button data-hist="${i}">${esc(x.label||x.type)}</button>`).join('')}</div>`}
async function openContext(desc,opts={}){if(!opts.fromHistory){ctxHistory.push({...desc,label:desc.name||desc.description||desc.product_code||desc.rut||desc.type});if(ctxHistory.length>8)ctxHistory.shift()}let wrap=document.getElementById('mpCtx');if(!wrap){wrap=document.createElement('div');wrap.id='mpCtx';wrap.className='mp-ctx';wrap.innerHTML='<div class="mp-ctx-panel"><div class="empty">Cargando contexto…</div></div>';document.body.appendChild(wrap)}wrap.querySelector('.mp-ctx-panel').innerHTML='<div class="empty">Construyendo contexto del índice…</div>';try{const payload={entity_type:desc.type};if(desc.type==='proveedor'||desc.type==='organismo'){payload.rut=desc.rut||'';payload.name=desc.name||''}else{payload.product_code=desc.product_code||'';payload.description=desc.description||''}renderContext(await analysisApi('context',payload))}catch(e){wrap.querySelector('.mp-ctx-panel').innerHTML=`<div class="fatal"><strong>No fue posible construir el contexto.</strong><div style="margin-top:7px">${esc(e.message||e)}</div><button id="mpCtxCloseErr" class="ghost" style="margin-top:10px">Cerrar</button></div>`;document.getElementById('mpCtxCloseErr').onclick=()=>wrap.remove()}}
function renderContext(d){const e=d.entity||{},wrap=document.getElementById('mpCtx'),panel=wrap?.querySelector('.mp-ctx-panel');if(!panel)return;const typeName={proveedor:'Proveedor',organismo:'Servicio público / comprador',item:'Producto / ítem'}[e.type]||e.type;panel.innerHTML=`<div class="mp-ctx-head"><div class="mp-ctx-title"><div><div class="mp-ctx-kind">${esc(typeName)}</div><h2>${esc(e.label||'Contexto')}</h2><div class="tiny">${e.rut?'RUT '+esc(e.rut):''}${e.product_code?'UNSPSC '+esc(e.product_code):''} · cobertura ${esc(fmtDate(e.min_date))} → ${esc(fmtDate(e.max_date))}</div>${contextTrail()}</div><div class="mp-ctx-actions"><span class="pill ok">descriptivo</span><button id="mpCtxClose" class="ghost">Cerrar</button></div></div></div><div class="mp-ctx-body"><div class="mp-ctx-kpis"><div class="mp-kpi"><div class="n">${fmtN(e.tender_count)}</div><div class="l">Procesos</div></div><div class="mp-kpi"><div class="n">${fmtN(e.buyer_count)}</div><div class="l">Compradores</div></div><div class="mp-kpi"><div class="n">${fmtN(e.supplier_count)}</div><div class="l">Proveedores vinculados</div></div><div class="mp-kpi"><div class="n">${fmtN(e.award_count)}</div><div class="l">Adjudicaciones</div></div><div class="mp-kpi"><div class="n">${fmtN((d.top_items||[]).length)}</div><div class="l">Categorías destacadas</div></div></div><div class="mp-ctx-grid"><div class="mp-box"><h4>Evolución temporal</h4>${barChart(d.timeline)}</div>${ctxRank(d.top_buyers,'organismo','Principales compradores')}${ctxRank((d.top_suppliers||[]).filter(x=>!(e.rut_norm&&x.key===e.rut_norm)),'proveedor','Principales proveedores')}</div><div class="mp-ctx-grid">${ctxRank(d.top_items,'item','Productos / categorías')}<div class="mp-box"><h4>Estados de los procesos</h4><div class="mp-mini-dist">${(d.status_distribution||[]).slice(0,9).map(x=>`<span class="mp-dist">${esc(x.key)} · ${esc(x.count)}</span>`).join('')}</div></div><div class="mp-box"><h4>Modalidades</h4><div class="mp-mini-dist">${(d.method_distribution||[]).slice(0,9).map(x=>`<span class="mp-dist">${esc(x.key)} · ${esc(x.count)}</span>`).join('')}</div></div></div>${d.award_amounts?.length?`<div class="mp-ctx-section"><h3>Montos adjudicados observados en esta cobertura</h3>${moneyBlocks(d.award_amounts)}<div class="tiny" style="margin-top:7px">Montos descriptivos por moneda. No se convierten ni se utilizan para generar indicadores de riesgo.</div></div>`:''}<div class="mp-ctx-section"><h3>Procesos relacionados · abrir evidencia oficial en vivo</h3><div class="mp-proc-table"><table><thead><tr><th>Código</th><th>Proceso</th><th>Comprador</th><th>Publicación</th><th>Estado</th></tr></thead><tbody>${(d.tenders||[]).map(t=>`<tr data-live="${esc(t.tender_code)}"><td class="code">${esc(t.tender_code)}</td><td><strong>${esc(t.title||'—')}</strong><div class="tiny">${esc(t.procurement_method_details||'')}</div></td><td>${esc(t.buyer_name||'—')}</td><td>${esc(fmtDate(t.publication_date))}</td><td>${esc(t.status_details||t.status||'')}</td></tr>`).join('')}</tbody></table></div></div><div class="mp-coverage-note">Esta vista describe relaciones observadas en el índice piloto. No clasifica riesgos ni detecta anomalías. Al abrir un proceso, el detalle se consulta desde la API oficial.</div></div>`;document.getElementById('mpCtxClose').onclick=()=>wrap.remove();panel.querySelectorAll('[data-live]').forEach(r=>r.onclick=()=>window.openCode(r.dataset.live));panel.querySelectorAll('[data-hist]').forEach((b,i)=>b.onclick=()=>{const target=ctxHistory[ctxHistory.length-Math.min(5,ctxHistory.length)+i];if(target){ctxHistory=ctxHistory.slice(0,ctxHistory.indexOf(target)+1);openContext(target,{fromHistory:true})}});bindPivots(panel)}
function install(){const shell=document.querySelector('.shell'),top=document.querySelector('.top'),workspace=document.querySelector('.workspace');if(!shell||!top||!workspace||document.getElementById('mpAnalystSearch'))return false;document.getElementById('mpSearchCard')?.remove();const search=document.createElement('section');search.id='mpAnalystSearch';search.className='mp-analyst-search';search.innerHTML=`<h2>Explorar Mercado Público</h2><p>Parte por una entidad o concepto. La herramienta reconstruye su contexto dentro del índice y permite pivotear entre proveedores, organismos, productos y procesos sin exigir una fecha.</p><div class="mp-searchline"><input id="mpAnalystQ" placeholder="Proveedor o RUT · servicio público · producto/UNSPSC · código o título"><button id="mpAnalystBtn" class="primary">Buscar</button></div><div class="mp-modes"><button class="mp-mode active" data-mode="all">Todo</button><button class="mp-mode" data-mode="proveedor">Proveedores</button><button class="mp-mode" data-mode="organismo">Servicios públicos</button><button class="mp-mode" data-mode="item">Productos / ítems</button><button class="mp-mode" data-mode="licitacion">Licitaciones</button></div>`;top.insertAdjacentElement('afterend',search);const pan=document.createElement('section');pan.id='mpPanorama';pan.className='mp-panorama';search.insertAdjacentElement('afterend',pan);document.getElementById('mpAnalystBtn').onclick=doSearch;document.getElementById('mpAnalystQ').onkeydown=e=>{if(e.key==='Enter')doSearch()};search.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.mode;search.querySelectorAll('[data-mode]').forEach(x=>x.classList.toggle('active',x===b))});const side=workspace.querySelector('.side');if(side){const h=side.querySelector('h3');if(h)h.textContent='Exploración por fecha';const tiny=side.querySelector('.tiny');if(tiny)tiny.textContent='Ruta secundaria para revisar publicaciones de un día específico.'}const brand=top.querySelector('.brand p');if(brand)brand.textContent='Piloto independiente · exploración relacional + fuente oficial en vivo';loadOverview();return true}
function wait(){if(install())return;const mo=new MutationObserver(()=>{if(install())mo.disconnect()});mo.observe(document.documentElement,{subtree:true,childList:true});setTimeout(()=>mo.disconnect(),120000)}
wait();
})();